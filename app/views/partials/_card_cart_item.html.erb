  <li class="list-group-item d-flex justify-content-between align-items-start">
    <div class="ms-2 me-auto">
          <%= link_to "#{item.title}", item_path(item), class: "neonText" %>
      <p class="neonText"><%= item.limb.name + "," + item.material + "," + item.control_type %></p>
    </div>
    <p class="neonText"><%= number_to_currency(item.price, unit: "¥") %></p>
  </li>